#include<stdio.h>
int main() {
    int a;
    printf("enter the value of side of quare:");
    scanf("%d",&a);
    int area =a * a;
    printf("the area of quare is:%d\n",area);
    return 0;

}